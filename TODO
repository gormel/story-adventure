fea: scene editor
 * edit component (popup window)
 ** поддержать редактирование
 ** заголовок с именем компонента и редактируемой entity
 * add component (popup window)
 * movement
 * entity tree
 * separate editor component list (mb extract categories)
fea: flipbook animations
ref: resources to @embedFile()
fea: tween animations